Program {
    int prinInt(int a) extern;

    int mult(int l, int o){
        int z;
        z = l * o;
        return z;
    }

    int suma(int a, int b) {
        return a + b;
    }


    int main() {
        int x;
        int y;
        x = 1;
        y = 2;
        while (x < 10) {
            prinInt(x);
            x = x + 1;
            y = mult(y,2);
            prinInt(y);
        }
        return 0;
    }
}
