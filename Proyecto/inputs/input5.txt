Program {

    bool fa;

    int inputVal() extern;
    int prinInt(int a) extern;

    void extraFunc(int a) {
        a = a+10;
    }

    bool main() {
        int res;
        bool aux;

        aux = true;
        res = inputVal();
        fa = res == 5;
        if(aux && fa)then{
            prinInt(res);
            return true;
        }else {
            int error;
            prinInt(error);
            return ;
        }
    }
}
