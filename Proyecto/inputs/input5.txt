Program {

    bool fa;

    int inputVal() extern;
    int prinInt(int a) extern;

    int extraFunc(int a) {
        a = a+10;
        return a;
    }

    void main() {
        int res;
        bool aux;

        aux = true;
        res = inputVal();
        fa = res == 5;
        if(aux && fa)then{
            prinInt(res);
        }else {
            int error;
            error = extraFunc(res);
            prinInt(error);
        }
    }
}
