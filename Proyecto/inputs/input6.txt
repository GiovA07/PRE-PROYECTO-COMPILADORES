Program {

    int s;
    int inc(int x) {
        int z;
        x = 3;
        z = 10 + 10;
        return 1;
    }
    bool get_bool() extern;

    void print_int(int i,int x, bool z) extern;

    int main() {
        int w;
        if (get_bool()) then {
            int xx;
        }
        xx = 10; 
        get_bool();
        print_int(1,2,get_bool());
        return 1;
    }
}

